# Reports

This directory contains the saved results of checklist runs.

When you run a template from the dashboard ("Run") or via `clf-play --template`, the completed checklist is saved here as a YAML report file. You can re-open any report from the dashboard ("View") or directly via `clf-play <report-file>`.
